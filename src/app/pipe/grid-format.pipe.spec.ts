import { GridFormatPipe } from './grid-format.pipe';

describe('GridFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GridFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
