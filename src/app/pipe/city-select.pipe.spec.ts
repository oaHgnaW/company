import { CitySelectPipe } from './city-select.pipe';

describe('CitySelectPipe', () => {
  it('create an instance', () => {
    const pipe = new CitySelectPipe();
    expect(pipe).toBeTruthy();
  });
});
